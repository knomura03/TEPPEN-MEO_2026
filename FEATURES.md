# TEPPEN MEO - 機能一覧 (Current Features)

TEPPEN MEOは、店舗オーナーやMEO対策代理店向けの、SNSおよびGoogleビジネスプロフィール統合管理プラットフォームです。

## ステータス凡例（このファイルの読み方）
- `[実装済み]`：現状そのまま動く（ただしフロント内の範囲）
- `[モック]`：UI/固定データ/ローカル状態のみ（実運用は不可）
- `[MVP予定]`：MVPで実装予定（`docs/00_MVP_DEFINITION.md`）
- `[将来]`：MVP後（`IMPROVEMENTS.md`）

## 1. 認証・ユーザー管理 (Authentication & User Management)

### 1.1 認証システム
- **簡易ログイン**: `[モック]` デモ用にAdmin, Manager, Userの3ロールへワンクリックで切り替え可能なログイン画面。
- **セッション管理**: `[モック]` `localStorage` を利用した簡易セッション維持。
- **ログアウト**: `[モック]` ログアウト操作はあるが、認証基盤は未実装。

### 1.2 権限ロール (Role Based Access Control)
- **Admin (開発者/全権管理者)**: `[モック]` 表示制御のみ（DB/RLSによる拒否は未実装）。
- **Manager (代理店/販売者)**: `[モック]` 表示制御のみ。
- **User (店舗オーナー)**: `[モック]` 表示制御のみ。

### 1.3 ユーザー・契約管理機能
- **ユーザー一覧表示**: `[モック]` 固定データの一覧表示。
- **KPI統計**: `[モック]` 固定値の表示。
- **CSVエクスポート**: `[モック]` 現状はコンソール出力 + 通知のみ（ファイルDLは未実装）。
- **編集・削除**: `[モック]` UIのみ（永続化なし）。

## 2. ダッシュボード (Dashboard)

### 2.1 MEOパフォーマンス分析
- **主要KPIカード**: `[モック]`
  - マップ表示回数
  - ルート検索数
  - 通話クリック数
  - ウェブサイト遷移数
  - 前期間比の増減率（%）表示
- **期間フィルター**: `[モック]` 過去7日間 / 過去30日間 の切り替え（データは固定）。

### 2.2 視覚的データ表現
- **パフォーマンス推移グラフ (Area Chart)**: `[モック]` 固定データで可視化。
- **検索タイプ内訳 (Bar Chart)**: `[モック]` 固定データで表示。
- **店舗評価サマリー**: `[モック]` 固定値で表示。

## 3. 投稿管理 (Post Management)

### 3.1 新規投稿作成 (Post Creator)
- **マルチプラットフォーム投稿**: `[モック]` 先の選択UIのみ（実投稿は未実装）。
- **メディアアップロード**: `[モック]` ブラウザ内プレビューのみ（Storage保存は未実装）。
- **AIアシスタント (Google Gemini)**: `[実装済み]` APIキー設定があれば生成可能（投稿実行は別）。
  - トピックとトーンを指定してキャプションを生成
  - 生成中のローディング表示と通知
- **投稿プレビュー**: `[実装済み]` UIプレビュー
  - Instagramプレビュー
  - Google Businessプレビュー
- **スケジューリング**: `[モック]` 予約日時の入力UIのみ（ジョブ実行は未実装）。

### 3.2 投稿一覧 (Post List)
- **ステータス管理**: `[モック]` 固定データで表示。
- **フィルタリング**: `[モック]` UIはあるがデータは固定。
- **リスト表示**: `[モック]` 固定データで表示。
- **操作**: `[モック]` 編集・削除ボタンは未実装。

## 4. 運用カレンダー (Calendar)

### 4.1 カレンダービュー
- **月間表示**: `[モック]` 固定データ表示（遷移は動く）。
- **日本の祝日対応**: `[モック]` 簡易祝日データで表示。
- **投稿ステータス可視化**: `[モック]` 固定投稿データで色分け表示。
- **フィルタリング**: `[モック]` UIはあるが固定データ。
- **インタラクション**: `[モック]` クリックは `alert`（詳細/作成は未実装）。

## 5. 統合受信箱 (Unified Inbox)

### 5.1 メッセージ一元管理
- **クロスプラットフォーム**: `[モック]` 固定データで表示（実連携なし）。
- **フィルタリング**: `[モック]` UIのみ。
- **検索**: `[モック]` UIのみ。

### 5.2 返信機能
- **スレッド表示**: `[モック]` 固定データで表示。
- **返信アクション**: `[モック]` ローカル状態のみ更新（実送信なし）。
- **ステータス管理**: `[モック]` ローカル状態のみ。

## 6. 設定・アカウント管理 (Settings)

### 6.1 プロフィール・プラン
- **基本情報**: `[モック]` 画面入力のみ（永続化なし）。
- **プラン情報**: `[モック]` 固定表示。

### 6.2 店舗情報設定 (MEO対策)
- **NAP情報管理**: `[モック]` 画面入力のみ（永続化なし）。
- **詳細情報**: `[モック]` 画面入力のみ。

### 6.3 SNS連携設定
- **アカウント接続**: `[モック]` トグルUIのみ（OAuth/実連携なし）。
- **ステータス表示**: `[モック]` ローカル状態のみ。

### 6.4 システム管理 (Admin Only)
- **API設定**: `[モック]` 入力UIのみ（実際のキー管理は未実装）。
- **メンテナンス**: `[モック]` ボタンUIのみ。

## 7. UI/UX & 基盤機能

### 7.1 デザインシステム
- **モダンUI**: `[実装済み]` Tailwind CSSを使用したレスポンシブデザイン。
- **ダークモード**: `[実装済み]` 全画面対応のダークテーマ切替。
- **アニメーション**: `[実装済み]` フェードイン、トースト通知のスライドイン。

### 7.2 通知センター
- **アプリ内通知**: `[実装済み]` トースト通知。
- **通知履歴**: `[実装済み]` ベルアイコンから履歴表示・既読管理。
- **ブラウザ通知連携**: `[実装済み]` ブラウザの許可が必要。

### 7.3 オンボーディング
- **ツアー機能**: `[実装済み]` 初回ユーザー向けガイド表示（既読は `localStorage`）。
